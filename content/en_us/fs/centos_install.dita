<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task outputclass="docs one-sidebar sidebar-first" id="taskId">
    <title>Install CentOS</title>
    <shortdesc/>
    <taskbody>
        <context>
            <!--<p></p>-->
        </context>
        <steps>
        	<step><cmd>Make sure the CentOS CD is in the CD drive and boot the computer from the CD drive.</cmd></step>
        	<step>
        		<cmd>At the boot prompt, press ENTER for the graphical installer. You can skip the media verification.</cmd>
        	</step>
        	
        	<step><cmd>Accept the defaults, except for the network interface configuration. At the page
        		where you set your hostname, click the <uicontrol>Configure Network</uicontrol> button.</cmd>
        		<stepresult>The Network Connections window displays.</stepresult>
        		</step>
        		<step>
        			<cmd>In the Network Connections window, select the network interface and click <uicontrol>Edit</uicontrol>.</cmd>
        			<stepresult>The <uicontrol>Editing System</uicontrol> window for the connection displays.</stepresult>
        		</step>
        	<step>
        		<cmd>In the Editing System window, click <uicontrol>Connect automatically</uicontrol>.</cmd>
        	</step>
        	<step>
        		<cmd>Click the <uicontrol>IPv4 Settings</uicontrol> tab and do the following:</cmd>
        		<substeps>
        			<!-- David K says this should be Manual...Manual is the default. Scot M tested with
        				Automatic.
        				<substep>
        				<cmd>In the <uicontrol>Method</uicontrol> drop-down, select <uicontrol>Automatic</uicontrol>.</cmd>
        			</substep>-->
        			<substep>
        				<cmd>In the <uicontrol>Address</uicontrol> area, click <uicontrol>Add</uicontrol> and
        					fill in the machine's IP address, netmask, and gateway.</cmd>
        			</substep>
        			<substep>
        				<cmd>In the <uicontrol>DNS servers</uicontrol> field, enter a URL for a DNS server (for example, 8.8.8.8).</cmd>
        			</substep>
        		</substeps>
        	</step>
        <step>
        	<cmd>Click <uicontrol>Apply</uicontrol> and finish with the installation.</cmd>
        </step>
        <step>
        	<cmd>Repeat on each machine you will use for FastStart.</cmd>
        </step>	
        
        </steps>
    	<postreq>You are now ready to <xref href="faststart_install_node.dita">install the FastStart Node</xref>.</postreq>
    </taskbody>
</task>
