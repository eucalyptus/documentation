<project>
      <param name="OUTEXT" expression="${out.ext}" if="out.ext" />
      <param name="doAncestors" expression="${ancestors.doAncestors}" if="ancestors.doAncestors"/>      
      <param name="includeLinkToIndexAsRootAncestor" expression="${ancestors.includeLinkToIndexAsRootAncestor}" if="ancestors.includeLinkToIndexAsRootAncestor"/>
      <param name="indexLink" expression="${welcome.page}" if="args.xhtml.toc" />
      <param name="indexLinkText" expression="${ancestors.indexLinkText}" if="ancestors.indexLinkText"/>	
</project>            